import 'package:flutter_test/flutter_test.dart';
import 'package:gallery_app/app/app.dart';
import 'package:gallery_app/main.dart';
import 'package:gallery_app/services/firebase_options.dart';
import 'package:gallery_app/ui/auth/auth_view_model.dart';
import 'package:gallery_app/app/app.locator.dart';

void main() {
  //setUp(() => const MyApp());
/*
  group('AuthValidationVmTest - ', () {
    group('forgotPassword - ', () {
      test('When invalid email is provided forgotPassword should be false', () {
        var model = AuthViewModel();

        var email = "";

        expect(model.forgotPassword(email: email), false);
      });
    });
  });*/
}

/*void main() {
  testWidgets('Add pulls up a dialog', (WidgetTester tester) async {
    // Build our app and trigger a frame.
    await tester.pumpWidget(const MyApp());

    // Tap the '+' icon and trigger a frame.
    await tester.tap(find.byIcon(Icons.add));
    await tester.pump();

    // Verify that our add results in a Dialog.
    expect(find.byType(Dialog), findsNothing);
    expect(find.byType(Dialog), findsOneWidget);
  });
}*/
